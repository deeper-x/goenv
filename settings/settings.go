package settings

// DefaultFile if not passed, this is the default: current dir, named .env
const DefaultFile = ".env"

// RegexRow expected row format
const RegexRow = `[a-zA-Z0-9\-\_]+[\=]{1}[a-zA-Z0-9\-\_]+`
